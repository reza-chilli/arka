const mongoose = require('mongoose');
// const userModel = mongoose.model('user');

module.exports = {
    userLogin: async (data) => {
        try {
            
        } catch (error) {
            
        }
    }
};